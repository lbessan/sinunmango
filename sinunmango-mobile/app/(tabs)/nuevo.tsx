// Esta pantalla no se usa directamente — el FAB navega a /nuevo-modal
// Se mantiene para que expo-router no rompa la estructura de tabs
import { View } from 'react-native'
export default function NuevoPlaceholder() {
  return <View />
}
